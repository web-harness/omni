language: typescript
layer: library
stack: frontend

tasks:
  clean-public:
    command: shx
    args:
      - rm
      - -f
      - ../../../omni-ui/public/omni-dockbar.js
      - ../../../omni-ui/public/omni-dockbar-*.js
    inputs:
      - '@group(configs)'
    options:
      internal: true
  build:
    command: esbuild
    args:
      - src/omni-dockbar.ts
      - --bundle
      - --format=esm
      - --splitting
      - --outdir=../../../omni-ui/public
      - --entry-names=omni-dockbar
      - --chunk-names=omni-dockbar-[name]-[hash]
    deps:
      - clean-public
    inputs:
      - '@group(sources)'
      - '@group(configs)'
  watch:
    command: esbuild
    args:
      - src/omni-dockbar.ts
      - --bundle
      - --format=esm
      - --splitting
      - --outdir=../../../omni-ui/public
      - --entry-names=omni-dockbar
      - --chunk-names=omni-dockbar-[name]-[hash]
      - --watch
    inputs:
      - '@group(sources)'
      - '@group(configs)'
    preset: server
