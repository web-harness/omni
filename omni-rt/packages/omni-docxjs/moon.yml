language: typescript
layer: library
stack: frontend

tasks:
  clean-public:
    command: shx
    args:
      - rm
      - -f
      - ../../../omni-ui/public/omni-docxjs.js
      - ../../../omni-ui/public/omni-docxjs-*.js
    inputs:
      - '@group(configs)'
    options:
      internal: true
  build:
    command: esbuild
    args:
      - src/omni-docxjs.ts
      - --bundle
      - --format=esm
      - --splitting
      - --outdir=../../../omni-ui/public
      - --entry-names=omni-docxjs
      - --chunk-names=omni-docxjs-[name]-[hash]
    deps:
      - clean-public
    inputs:
      - '@group(sources)'
      - '@group(configs)'
    options:
      shell: false
  watch:
    command: esbuild
    args:
      - src/omni-docxjs.ts
      - --bundle
      - --format=esm
      - --splitting
      - --outdir=../../../omni-ui/public
      - --entry-names=omni-docxjs
      - --chunk-names=omni-docxjs-[name]-[hash]
      - --watch
    deps:
      - clean-public
    inputs:
      - '@group(sources)'
      - '@group(configs)'
    preset: server
    options:
      shell: false