{
  "name": "@omni/omni-dock",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/omni-dock.ts --bundle --format=esm --outfile=dist/omni-dock.js --loader:.css=text --loader:.svg=text",
    "watch": "esbuild src/omni-dock.ts --bundle --format=esm --outfile=dist/omni-dock.js --loader:.css=text --loader:.svg=text --watch"
  },
  "dependencies": {
    "dockview-core": "^5.1.0",
    "lit": "^3.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.2"
  }
}
